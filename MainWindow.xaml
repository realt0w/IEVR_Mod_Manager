<Window x:Class="IEVRModManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="IEVR Mod Manager 1.6" 
        Height="900" Width="1400"
        MinHeight="600" MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BgBrush}">
    <Window.Resources>
        <Style x:Key="SuccessAccentButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource AccentButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource SuccessBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SuccessBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SuccessHoverBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource SuccessHoverBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SuccessActiveBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource SuccessActiveBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{DynamicResource DisabledBgBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource DisabledBgBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource DisabledTextBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" MinHeight="450"/>
            <RowDefinition Height="200" MaxHeight="200"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <Label x:Name="TitleLabel" Grid.Row="0" Content="IE:VR Mod Manager 1.6" 
               FontSize="26" FontWeight="Bold" 
               Foreground="{DynamicResource TextBrightBrush}"
               HorizontalAlignment="Center" 
               Margin="12,16,12,12"/>

        <!-- Center Frame: Mods List and Controls -->
        <Grid Grid.Row="1" Margin="16,0,16,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" MinWidth="400"/>
                <ColumnDefinition Width="*" MinWidth="180"/>
            </Grid.ColumnDefinitions>

            <!-- Mods Panel (Left) -->
            <Grid Grid.Column="0" Margin="0,0,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <GroupBox x:Name="ModsGroupBox" Grid.Row="0" Header="Mods" Padding="10">
                    <ListView x:Name="ModsListView" 
                              SelectionMode="Single"
                              MouseDoubleClick="ModsListView_MouseDoubleClick"
                              SelectionChanged="ModsListView_SelectionChanged">
                        <ListView.Resources>
                            <Style TargetType="GridViewColumnHeader">
                                <Setter Property="Background" Value="{StaticResource CardBgBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextBrightBrush}"/>
                                <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                <Setter Property="BorderThickness" Value="0,0,0,2"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="FontSize" Value="13"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="SnapsToDevicePixels" Value="True"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="GridViewColumnHeader">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"
                                                                  RecognizesAccessKey="True"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource HoverBgBrush}"/>
                                        <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                        <Setter Property="Foreground" Value="{StaticResource TextBrightBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.6"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.Resources>
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="✓" Width="60">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding EnabledIcon}" 
                                                       FontSize="14"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Enabled}" Value="True">
                                                                <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Mod Name" Width="280" DisplayMemberBinding="{Binding DisplayName}"/>
                                <GridViewColumn Header="Version" Width="80" DisplayMemberBinding="{Binding ModVersion}"/>
                                <GridViewColumn Header="Game Ver." Width="90" DisplayMemberBinding="{Binding GameVersion}"/>
                                <GridViewColumn Header="Author" Width="160" DisplayMemberBinding="{Binding Author}"/>
                                <GridViewColumn Header="Link" Width="240">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding ModLink}"
                                                       TextTrimming="CharacterEllipsis"
                                                       Cursor="Hand"
                                                       MouseLeftButtonUp="ModLink_Click">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=IsSelected}" Value="True">
                                                                <Setter Property="Foreground" Value="{StaticResource TextBrightBrush}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </GroupBox>

                <!-- Quick Actions -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="0,10,0,10">
                    <Button x:Name="ScanModsButton" Content="🔍 Scan Mods" Style="{StaticResource PrimaryButtonStyle}" 
                            Padding="18,11"
                            MinWidth="150"
                            Click="ScanMods_Click" Margin="0,0,6,0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"/>
                    <Button x:Name="OpenModsFolderButton" Content="📂 Open Mods Folder" Style="{StaticResource PrimaryButtonStyle}" 
                            Padding="18,11"
                            MinWidth="170"
                            Click="OpenModsFolder_Click" Margin="6,0,6,0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"/>
                    <Button x:Name="BrowseModsButton" Content="🧭 Browse mods" Style="{StaticResource PrimaryButtonStyle}" 
                            Padding="18,11"
                            MinWidth="150"
                            Click="OpenGameBananaBrowser_Click" Margin="6,0,10,0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,0">
                        <Label x:Name="VersionLabel" Content="Version:" Foreground="{StaticResource TextBrightBrush}" VerticalAlignment="Center" Margin="0,0,6,0" FontWeight="SemiBold"/>
                        <ComboBox x:Name="CpkSelector"
                                  Width="200"
                                  ItemsSource="{Binding}"
                                  DisplayMemberPath="DisplayName"
                                  SelectedValuePath="FileName"
                                  Foreground="{DynamicResource TextBrightBrush}"
                                  SelectionChanged="CpkSelector_SelectionChanged"
                                  Margin="0,0,0,0">
                            <ComboBox.ItemContainerStyle>
                                <Style TargetType="ComboBoxItem">
                                    <Setter Property="Foreground" Value="{StaticResource TextBrightBrush}"/>
                                </Style>
                            </ComboBox.ItemContainerStyle>
                            <ComboBox.Resources>
                                <Style TargetType="ToggleButton">
                                    <Setter Property="Padding" Value="12,8"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextBrightBrush}"/>
                                    <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="SnapsToDevicePixels" Value="True"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="4"
                                                        Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                      VerticalAlignment="Center"
                                                                      RecognizesAccessKey="True"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                                            <Setter Property="BorderBrush" Value="{StaticResource AccentHoverBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Background" Value="{StaticResource AccentActiveBrush}"/>
                                            <Setter Property="BorderBrush" Value="{StaticResource AccentActiveBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Opacity" Value="0.6"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                                <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}"
                                                 Color="{Binding Source={StaticResource TextBrightBrush}, Path=Color}"/>
                            </ComboBox.Resources>
                        </ComboBox>
                    </StackPanel>
                </StackPanel>

                <!-- Apply/Play Buttons -->
                <Grid Grid.Row="2" HorizontalAlignment="Stretch" Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="ApplyButton"
                            Grid.Column="0"
                            Content="✓ Apply Changes" 
                            Style="{StaticResource AccentButtonStyle}"
                            Click="ApplyMods_Click" 
                            HorizontalAlignment="Stretch"
                            Margin="0,0,8,0"/>
                    <Button x:Name="PlayButton"
                            Grid.Column="1"
                            Content="▶ Play"
                            Style="{StaticResource SuccessAccentButtonStyle}"
                            Click="PlayButton_Click"
                            HorizontalAlignment="Stretch"
                            Margin="8,0,0,0"/>
                </Grid>
            </Grid>

            <!-- Controls Panel (Right) -->
            <GroupBox x:Name="ActionsGroupBox" Grid.Column="1" Header="Actions" Padding="12">
                <StackPanel>
                    <!-- Profile Selector -->
                    <ComboBox x:Name="ProfileSelector"
                              Margin="0,0,0,10"
                              SelectionChanged="ProfileSelector_SelectionChanged"
                              Foreground="{DynamicResource TextBrightBrush}">
                        <ComboBox.ItemContainerStyle>
                            <Style TargetType="ComboBoxItem">
                                <Setter Property="Foreground" Value="{StaticResource TextBrightBrush}"/>
                            </Style>
                        </ComboBox.ItemContainerStyle>
                        <ComboBox.Resources>
                            <Style TargetType="ToggleButton">
                                <Setter Property="Padding" Value="12,8"/>
                                <Setter Property="Foreground" Value="{StaticResource TextBrightBrush}"/>
                                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="SnapsToDevicePixels" Value="True"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="4"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"
                                                                  RecognizesAccessKey="True"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource AccentHoverBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter Property="Background" Value="{StaticResource AccentActiveBrush}"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource AccentActiveBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.6"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                            <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}"
                                             Color="{Binding Source={StaticResource TextBrightBrush}, Path=Color}"/>
                        </ComboBox.Resources>
                    </ComboBox>
                    <Button x:Name="ManageProfilesButton" Content="⚙️ Manage Profiles" Style="{StaticResource PrimaryButtonStyle}" 
                            Click="ManageProfilesButton_Click" Margin="0,0,0,10" HorizontalAlignment="Stretch"/>
                    
                    <Button x:Name="MoveUpButton" Content="⬆️ Move Up" Style="{StaticResource PrimaryButtonStyle}" 
                            Click="MoveUp_Click" Margin="0,0,0,6" HorizontalAlignment="Stretch"/>
                    <Button x:Name="MoveDownButton" Content="⬇️ Move Down" Style="{StaticResource PrimaryButtonStyle}" 
                            Click="MoveDown_Click" Margin="0,0,0,10" HorizontalAlignment="Stretch"/>
                    
                    <Button x:Name="EnableAllButton" Content="✅ Enable All" Style="{StaticResource PrimaryButtonStyle}" 
                            Click="EnableAll_Click" Margin="0,0,0,6" HorizontalAlignment="Stretch"/>
                    <Button x:Name="DisableAllButton" Content="❌ Disable All" Style="{StaticResource PrimaryButtonStyle}" 
                            Click="DisableAll_Click" HorizontalAlignment="Stretch"/>
                </StackPanel>
            </GroupBox>
        </Grid>

        <!-- Log Panel -->
        <GroupBox x:Name="ActivityLogGroupBox" Grid.Row="2" Header="Activity Log" Margin="16,8,16,12" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBox x:Name="LogTextBox" 
                         Grid.Row="0"
                         IsReadOnly="True" 
                         TextWrapping="Wrap"
                         FontFamily="{StaticResource FontMono}"
                         FontSize="11"
                         Background="{DynamicResource BgBrush}"
                         Foreground="{DynamicResource TextBrush}"
                         BorderThickness="0"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         Padding="8"/>

            </Grid>
        </GroupBox>

        <!-- Bottom Frame: Downloads, Configuration, Exit -->
        <Grid Grid.Row="3" Margin="16,0,16,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="ConfigurationButton" Grid.Column="0" Content="⚙️ Configuration" 
                    Style="{StaticResource PrimaryButtonStyle}" 
                    Click="Configuration_Click" 
                    Width="190" Margin="0,0,8,0"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0,0,0">
                <Button x:Name="LinksButton" Content="📥 Links" 
                        Style="{StaticResource PrimaryButtonStyle}" 
                        Click="Downloads_Click" 
                        Width="190" Margin="0,0,10,0"/>
                <TextBlock x:Name="HelpLink" Text="📖 Help" 
                           Foreground="{StaticResource AccentBrush}"
                           FontSize="13"
                           FontWeight="SemiBold"
                           Cursor="Hand"
                           VerticalAlignment="Center"
                           MouseEnter="HelpLink_MouseEnter"
                           MouseLeave="HelpLink_MouseLeave"
                           MouseLeftButtonDown="HelpLink_Click"/>
            </StackPanel>
            <Button x:Name="CopyLogButton" Grid.Column="3" Content="Copy Log" 
                    Style="{StaticResource PrimaryButtonStyle}" 
                    Padding="18,11"
                    MinWidth="120"
                    Click="CopyLog_Click" 
                    Margin="8,0,8,0"/>
            <Button x:Name="SaveLogButton" Grid.Column="4" Content="Save Log" 
                    Style="{StaticResource PrimaryButtonStyle}" 
                    Padding="18,11"
                    MinWidth="120"
                    Click="SaveLog_Click" 
                    Margin="0,0,8,0"/>
            <Button x:Name="ExitButton" Grid.Column="5" Content="✕ Exit" 
                    Style="{StaticResource PrimaryButtonStyle}" 
                    Click="Exit_Click" 
                    Width="150"/>
        </Grid>
    </Grid>
</Window>

